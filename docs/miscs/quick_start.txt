1. Install python 3.11
2. Install miniconda (virtual environment/package manager -> https://www.anaconda.com/docs/getting-started/miniconda/install)
3. conda env create -f infra/environment.yml #()
 W3 py and dj
https://docs.djangoproject.com/en/6.0/intro/tutorial1/



python3 manage.py flush -> flush the DB, delete users etc.
python manage.py makemigrations BackendAPI -> prepare the translators between the models and the DB
python manage.py migrate -> prepare the DB so that it can accomodate the models (enable to have changing models and backcompatibility)
python manage.py runserver -> launches the Backend

